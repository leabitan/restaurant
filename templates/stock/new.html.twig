{% extends 'base.html.twig' %}
{% block body %}
<div class="container" style="margin-top: 15vh;margin-bottom: 15vh;">
    <h1 class="mt-4">Ajouter un stock</h1>

    {% for message in app.flashes('success') %}
    <div class="alert alert-success mt-4">
        {{ message }}
    </div>
    {% endfor %}

    {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.quantityPrepared) }}
            {{ form_widget(form.quantityPrepared) }}
            <div class="form-error">
                {{ form_errors(form.quantityPrepared) }}
            </div>
        </div>

        {# <div class="form-group">
            {{ form_label(form.quantityReal) }}
            {{ form_widget(form.quantityReal) }}
            <div class="form-error">
                {{ form_errors(form.quantityReal) }}
            </div>
        </div> #}

        <div class="form-group">
            {{ form_label(form.quantitySold) }}
            {{ form_widget(form.quantitySold) }}
            <div class="form-error">
                {{ form_errors(form.quantitySold) }}
            </div>
        </div>

        {# <div class="form-group">
            {{ form_row(form.submit) }}
        </div> #}

    {{ form_end(form) }}

</div>
{% endblock %}